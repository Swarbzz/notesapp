  // function testaddnote() {
  //   var note = new Note
  //   assert.isTrue(note.text)
  // }

  // testaddnote()

  function testNote() {
    var note = new Note();
    var text = text;
    assert.isTrue(note.text === text);
  };
  
 testNote();